name: Build Backend with SWC

on:
  push:
    branches: [develop]

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      mongo:
        image: mongo:6.0
        ports:
          - 27017:27017
        options: >-
          --health-cmd="mongosh --eval 'db.adminCommand(\"ping\")'"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    env:
      NODE_ENV: test
      MONGO_DB: chat
      MONGO_DB_URI: "mongodb://localhost:27017/"
      MONGO_DB_USER: ""
      MONGO_DB_PASSWD: ""

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies
        working-directory: ./backend
        run: npm ci

      - name: Build Project (SWC)
        working-directory: ./backend
        run: npm run build

      - name: Start & Test App
        working-directory: ./backend
        run: |
          node dist/src/server.js &
          sleep 10
          echo "✅ Backend server started and connected to local MongoDB"
